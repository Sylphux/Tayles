<%= form_with(model: premium) do |form| %>
  <% if premium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(premium.errors.count, "error") %> prohibited this premium from being saved:</h2>

      <ul>
        <% premium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price_cents, style: "display: block" %>
    <%= form.number_field :price_cents %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
