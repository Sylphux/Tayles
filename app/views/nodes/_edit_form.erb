<%= form_with(model: node, class: "node-form") do |form| %>

  <% if node.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(node.errors.count, "error") %> prohibited this node from being saved:</h2>

      <ul>
        <% node.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%# <%= form.label :node_title, "Title" %>
    <%= form.text_field :node_title, class: "title-input" %>
  </div>
  <div>
    <%# <%= form.label :public_description, "Node description" %>
    <%= form.text_area :public_description, class: "description-text-input"%>
  </div>
  <div>
    <%= form.submit "Update Node", class: "input-btn" %>
  </div>

<% end %>
